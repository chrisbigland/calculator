const scoresArr = [1,2,3]

const totalScores = (scoreArr) => {
    let scoresSum = 0;
    for (let i = 0; i < scoreArr.length; i++) {
      scoresSum += scoreArr[i];
     
    }
  
}
      
console.log(totalScores())